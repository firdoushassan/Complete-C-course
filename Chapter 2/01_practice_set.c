# include <stdio.h>

int main(){
    // number divisible by 97 or not!
    int num;
    printf("Enter the number\n", num);
    scanf("%d", &num);
    printf("Divisibility test returns %d", num%97);
return 0;
}