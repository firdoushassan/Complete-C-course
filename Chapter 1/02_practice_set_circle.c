#include <stdio.h>

int main()
{
    int radius = 3;
    float pi = 3.14;
    printf("The area of the circle is %f sq.units \n", pi * radius * radius);
    int height = 3;
    printf("Volume of the cylinder is %f cube.units", pi * radius * radius * height);

    return 0;
}