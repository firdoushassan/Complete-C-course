# include <stdio.h>

int main(){
     char s[20];
    printf("Enter the string \n", s);
    gets(s); //used to scan it(multiple words)
    puts(s); //used to print it
    // printf("Your entered string is %s", s);
return 0;
}