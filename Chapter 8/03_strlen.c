#include <stdio.h>
#include <string.h>

int main()
{
    char *st = "hello";
    int a = strlen(st); //Tells the number of words in the string
    printf("The length of the string is %d", a);
    return 0;
}